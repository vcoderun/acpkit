interface:
  display_name: "LangChain ACP"
  short_description: "Expose LangGraph and DeepAgents through ACP"
  default_prompt: "Use $langchain-acp to update LangChain ACP graph adaptation, plan extraction, bridges, or projection behavior."

policy:
  allow_implicit_invocation: true
